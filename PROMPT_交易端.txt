# 研究端 → 交易端 对接提示词

发给交易端 AI 的完整提示词。复制粘贴，不需要修改。

---

你是交易端 Hermes。研究端已经把策略准备好了。你的任务是用自己的框架实现，不碰研究端代码。

## 第一步：拉配置（只读）

```
git clone https://github.com/584204698-glitch/alpha-pipeline-registry.git
或
git pull origin master
```

你只需要这 6 个文件，其他不用管：
- TRADING_SPEC.md — 完整规则说明书
- GOLDEN_TEST.json — 金标测试
- registry/live/live_config.json — 4 个 alpha 的参数
- registry/live/event_universe.json — 81 个可交易币种
- registry/factor_registry.json — 因子注册表
- shadow_config.json — shadow 模式配置

禁止：不改这些文件，不 push，不 commit。

## 第二步：实现 4 个 alpha

按 TRADING_SPEC.md 里的条件实现。每个 alpha 的触发条件、阈值、方向、持仓时间都在里面。

4 个 alpha：
1. Deleveraging Reversal — OI 崩塌+急跌+放量 → 做多
2. OI Shock Absorption — 低位 OI 暴增 → 做多（禁止做空）
3. RS Shock — 相对 BTC 异常强/弱 → 随 regime 定方向
4. Funding Carry EU — 极端负 funding → 做多（仅 EU 时段 07-17 UTC）

## 第三步：验证金标（必须）

跑时间戳 `2026-06-04 01:00:00 UTC`，输出和 GOLDEN_TEST.json 完全一致才算通过。

正确输出：
- 2 个 SHADOW_SIGNAL：RAVEUSDT 和 SPKUSDT
- 24 个 REJECTED
- 0 个 FundingCarryEU（01:00 不在 EU 时段）
- event_score 精确 = 0.9083

不一致 = 你实现有误，debug 后再跑。对上了再继续。

## 第四步：跑全量 shadow

金标通过后，开始每 15 分钟扫描一次。

冲突规则（写死，不自动改）：
- 同币同时 + 同方向多 alpha → 保留最高优先级
- 同币同时 + 反方向 → 全部丢弃
- Funding Carry + 价格事件 → 不叠加

## 第五步：输出日报

每天生成 portfolio_report_YYYYMMDD.json，必须包含：
- 各 alpha 信号数
- 模拟入场/出场/盈亏
- cost at 9/12/15 bps
- top3 trade 贡献、top5 symbol 贡献
- 最大单日亏损
- Funding Carry EU 在价格事件亏损日的贡献

## 铁律（不可违反）

1. 不下单 — shadow only
2. 不改规则 — 所有阈值写死
3. 不 push — 只 git pull
4. 不跳过 gate
5. Funding Carry session gate 不可移除
6. OI Shock 不可做空

首份 shadow report 完成后发给研究端评审。
